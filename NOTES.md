Для начала я бы добавил в проект MapStruct для более удобного и наглядного маппинга сущностей в дто и обратно.
В контроллере TripController нужно поменять путь с "/api/v1/vehicles" на "/api/v1/trips".
Переработать структуру проекта: вынести реализации интерфейсов в отдельные папки; все, что с связано с исключениями вынести отдельно; убрать MongoSeedLoader из config; TelemetryEvent убрать из api/dto; разделить репозитории и сущности; поменять структуру папки service.